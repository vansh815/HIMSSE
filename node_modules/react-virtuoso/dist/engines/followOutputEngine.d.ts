import { TScrollLocation } from '../EngineCommons';
import { TObservable, TSubject } from '../tinyrx';
interface FollowOutputParams {
    totalCount$: TObservable<number>;
    scrolledToBottom$: TSubject<boolean>;
    scrollToIndex$: TSubject<TScrollLocation>;
}
export declare function followOutputEngine({ scrollToIndex$, scrolledToBottom$, totalCount$ }: FollowOutputParams): {
    followOutput$: TSubject<boolean>;
};
export {};
