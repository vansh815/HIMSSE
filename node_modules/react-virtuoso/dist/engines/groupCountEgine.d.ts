import { Transposer } from '../GroupIndexTransposer';
import { TSubject } from '../tinyrx';
interface GroupCountParams {
    totalCount$: TSubject<number>;
    stickyItems$: TSubject<number[]>;
    transposer$: TSubject<Transposer>;
}
export declare function groupCountEngine({ transposer$, stickyItems$, totalCount$ }: GroupCountParams): {
    groupCounts$: TSubject<number[]>;
    groupIndices$: import("../tinyrx").TObservable<number[]>;
};
export {};
