import { TObservable } from '../tinyrx';
import { ComponentType } from 'react';
export interface ListRange {
    startIndex: number;
    endIndex: number;
}
export interface ScrollSeekToggle {
    (velocity: number, range: ListRange): boolean;
}
export declare type TSeekPlaceholder = ComponentType<{
    height: number;
    index: number;
}>;
export interface ScrollSeekConfiguration {
    enter: ScrollSeekToggle;
    change: (velocity: number, range: ListRange) => void;
    exit: ScrollSeekToggle;
    placeholder: TSeekPlaceholder;
}
interface ScrollSeekParams {
    isScrolling$: TObservable<boolean>;
    scrollTop$: TObservable<number>;
    rangeChanged$: TObservable<ListRange>;
}
export declare function scrollSeekEngine({ isScrolling$, scrollTop$, rangeChanged$: range$ }: ScrollSeekParams): {
    isSeeking$: import("../tinyrx").TSubject<boolean>;
    scrollSeekConfiguration$: import("../tinyrx").TSubject<false | ScrollSeekConfiguration | undefined>;
    scrollVelocity$: import("../tinyrx").TSubject<number>;
};
export {};
